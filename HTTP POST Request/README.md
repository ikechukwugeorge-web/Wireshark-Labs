Captured an HTTP POST request generated during interaction with the OWASP Juice Shop lab. This demonstrates how client data is transmitted to a web server and how POST requests differ from GET requests.

Skills Demonstrated
HTTP POST
Form submission
Request body analysis
Web application traffic
